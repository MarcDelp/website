/**
 * Mobile values that will complement the 'overrides' field from
 * the base theme if the navigation is set in mobile mode
 */
export default {
  MuiTimelineOppositeContent: {
    root: {
      'max-width': '40%'
    }
  }
}
